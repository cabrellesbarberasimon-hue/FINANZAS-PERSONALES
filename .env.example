# Base de datos de TUS DATOS REALES (SQLite local). Ruta relativa a la raíz del proyecto.
DATABASE_URL="file:./data/finanzas.db"

# Modo de la aplicación: "real" (por defecto) o "demo".
# El modo demo SIEMPRE usa una base de datos separada (data/demo.db); ver `npm run demo:setup`.
APP_MODE="real"
